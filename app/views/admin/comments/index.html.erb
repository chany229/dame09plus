<table>
	<tr>
		<th></th>
		<th>用户</th>
		<th>评论</th>
		<th>操作</th>
	</tr>
	<% @comments.each do |comment| %>
		<tr>
			<td>
				<% if @last_readed_comment_id < comment.id %>
					new!
				<% end %>
			</td>
			<td>
				<% if comment.user %>
					<%= comment.user.username %>
				<% else %>
					匿名用户
				<% end %>
			</td>
			<td>
				<%= comment.comment %>
			</td>
			<td>
				回覆 删除
			</td>>
		</tr>
	<% end %>
</table>
